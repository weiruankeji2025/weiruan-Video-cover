# 快速启动指南

## 30秒快速体验

```bash
# 1. 安装依赖
npm install

# 2. 启动应用
npm start
```

应用将自动在浏览器中打开，**无需注册登录**，直接开始使用！🎉

## 第一次使用

### 1️⃣ 提取封面（30秒）

1. 点击"今日签到"（可选）
2. 选择输出质量
3. 拖拽视频文件到上传区域
4. 等待自动提取 5 张封面
5. 点击选择喜欢的封面
6. 点击"下载选中封面"

## 完整功能演示流程

### 场景：新用户完整体验

```
打开应用 → 签到 → 上传视频 → 选择封面 → 下载 → 查看面板
```

**预计时间：** 1 分钟

### 详细步骤

#### 步骤1：打开应用
- 运行 `npm start`
- 自动在浏览器中打开应用
- 以游客模式直接使用，无需登录

#### 步骤2：每日签到
- 点击"今日签到"按钮
- 查看连续签到天数
- 获得签到奖励（未来功能）

#### 步骤3：上传视频
- 选择输出质量（高品质/清晰/一般）
- 点击上传区域或拖拽文件
- 观察实时进度

#### 步骤4：选择封面
- 浏览 5 张自动提取的封面
- 点击选择最喜欢的一张
- 注意选中的封面会高亮显示

#### 步骤5：下载封面
- 点击"下载选中封面"
- 封面自动保存到下载文件夹
- 文件名包含质量和时间戳

#### 步骤6：查看个人面板
- 点击导航栏"个人面板"
- 查看统计数据：
  - 上传次数
  - 下载次数
  - 签到天数
  - 使用时长
- 查看使用趋势图
- 浏览历史记录

## 快速测试命令

### 测试功能模块

```bash
# 打开测试页面
open http://localhost:8080/test.html

# 或在浏览器访问
http://localhost:8080/test.html
```

### 清除测试数据

在浏览器控制台执行：

```javascript
// 清除所有数据
localStorage.clear();

// 刷新页面
location.reload();
```

## 常用操作

### 清除数据

```
1. 点击右上角"清除数据"
2. 确认清除操作
3. 页面自动刷新，所有数据重置
```

### 重新上传视频

```
1. 在封面预览页面点击"重新上传"
2. 或刷新页面返回上传页面
```

### 查看历史记录

```
1. 点击"个人面板"
2. 点击"上传历史"或"下载历史"
3. 浏览所有记录
```

## 推荐的测试视频

### 视频要求

- **格式：** MP4、AVI、MOV、WebM
- **大小：** 建议 10-50MB
- **时长：** 建议 30秒 - 5分钟
- **分辨率：** 任意

### 测试建议

1. **第一次测试：** 使用小视频（< 10MB）快速体验
2. **功能测试：** 使用中等视频（10-50MB）测试完整流程
3. **压力测试：** 使用大视频（50-200MB）测试性能

## 开发模式

### 修改代码后

```bash
# 无需重启服务器，直接刷新浏览器即可
```

### 调试技巧

1. **打开浏览器开发者工具：** F12 或 Ctrl+Shift+I
2. **查看控制台：** 检查 JavaScript 错误
3. **查看网络：** 检查资源加载情况
4. **查看存储：** Application → Local Storage

### 查看存储的数据

在浏览器控制台执行：

```javascript
// 查看所有用户
console.log(JSON.parse(localStorage.getItem('weiruan_users')));

// 查看当前用户
console.log(JSON.parse(localStorage.getItem('weiruan_current_user')));

// 查看签到数据
console.log(JSON.parse(localStorage.getItem('weiruan_checkin')));

// 查看统计数据
console.log(JSON.parse(localStorage.getItem('weiruan_stats')));
```

## 故障排除

### 问题：端口已被占用

```bash
# 使用其他端口
npx http-server -p 8081
```

### 问题：页面无法访问

```bash
# 检查服务器是否运行
ps aux | grep http-server

# 重新启动
npm start
```

### 问题：验证码看不清

```
点击验证码右侧的刷新按钮 🔄
```

### 问题：视频上传失败

```
1. 检查视频格式是否支持
2. 检查文件大小是否 < 4GB
3. 尝试其他视频文件
4. 查看浏览器控制台错误
```

## 性能优化建议

### 首次上传建议使用小视频

- 小视频提取速度快
- 便于快速验证功能
- 减少等待时间

### 选择合适的输出质量

- **高品质：** 适合高清视频，文件较大
- **清晰：** 平衡质量和文件大小
- **一般：** 文件小，适合快速预览

### 浏览器推荐

- Chrome（推荐，性能最佳）
- Firefox（良好支持）
- Safari（Mac 用户）
- Edge（Windows 用户）

## 快捷键

目前支持：

- **Enter：** 在登录/注册页面提交表单
- **F5：** 刷新页面
- **F12：** 打开开发者工具

## 下一步

1. ✅ 完成基础功能体验
2. 📊 查看个人数据面板
3. 🎨 体验不同输出质量
4. 📱 在移动设备上测试
5. 🔄 尝试连续多天签到
6. 📈 积累数据查看趋势图

## 获取帮助

- 📖 查看 [README.md](./README.md) 了解详细功能
- 🧪 查看 [TESTING.md](./TESTING.md) 了解测试方法
- 💡 遇到问题可以查看浏览器控制台错误信息

---

**开始体验威软视频封面提取器！** 🚀
